//WAPC to display the sum of the first n odd numbers and the first n even numbers separately where 'n' is the user input

#include<stdio.h>
int main()
{
    int num,sumEven=0,sumOdd=0;
    printf("\nEnter a number:");
    scanf("%d", &num);
    for(int i=1;i<=n;i+=2)
    {sumOdd+=i;}
    for9int i=2;i<=n;i+=2)
    {sumEven+=i}
    printf("\nThe sum of the first n odd numbers is %d",sumOdd);
    printf("\nThe sum of the first n even numbers is %d",sumEven);
    return 0;
}
    
